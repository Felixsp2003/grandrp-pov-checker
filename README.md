# Grand RP POV Checker V28

Workflow: YouTube-Upload 100% -> lokale OCR -> Prüfung -> finaler Dateiname.
Die manuelle Videoauswahl läuft über `manual.html` auf derselben GitHub-Pages-Origin und nutzt IndexedDB, damit kein `about:blank` verwendet wird.
Server wird für den aktuellen Grand-RP-Workflow fest auf 3 gesetzt; SC kann jederzeit aus dem Video geprüft werden.
